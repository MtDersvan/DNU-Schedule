'use strict';

const activities = require('./activities');
const groups = require('./groups');
const reglaments = require('./reglaments');
const teachers = require('./teachers');
const common = require('./common');
const users = require('./users');

module.exports = {
  activities,
  groups,
  reglaments,
  teachers,
  users,
  common
};
